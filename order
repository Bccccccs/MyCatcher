ssh likun@101.126.86.17
cd ..
cd ..
cd data/bc/MyCatcher

 python experiments/trickcatcher/run_trickcatcher.py
  for d in outputs/inputs/*; do [ -d "$d" ] && echo "$(basename "$d") $(find "$d" -maxdepth 1 -type f -name '*.in' | wc -l)"; done | sort
